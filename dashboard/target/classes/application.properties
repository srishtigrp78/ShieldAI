# Default Profile Configuration
spring.profiles.active=local

# H2 Database Configuration (Fallback/Testing)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update

# Gmail SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dashing.ayu.2378@gmail.com
spring.mail.password=ystm oooa ldls cemg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Server Configuration
server.port=8080

# Offline Storage Configuration
shieldai.offline.storage.enabled=true
shieldai.offline.storage.directory=${user.home}/.shieldai/offline
shieldai.offline.sync.interval=30000
shieldai.offline.sync.batch-size=100